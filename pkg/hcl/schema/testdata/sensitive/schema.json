{
  "name": "sensitive",
  "version": "0.0.0-dev",
  "config": {},
  "resources": {
    "sensitive:index:sensitive": {
      "properties": {
        "sensitiveOutput": {
          "type": "string",
          "secret": true
        },
        "sensitiveString": {
          "type": "string",
          "secret": true
        },
        "untypedValue": {
          "$ref": "pulumi.json#/Any",
          "description": "No type constraint, so it defaults to the any type."
        }
      },
      "type": "object",
      "required": [
        "sensitiveOutput",
        "sensitiveString"
      ],
      "inputProperties": {
        "sensitiveString": {
          "type": "string",
          "secret": true
        },
        "untypedValue": {
          "$ref": "pulumi.json#/Any",
          "description": "No type constraint, so it defaults to the any type."
        }
      },
      "requiredInputs": [
        "sensitiveString"
      ],
      "isComponent": true
    }
  }
}
