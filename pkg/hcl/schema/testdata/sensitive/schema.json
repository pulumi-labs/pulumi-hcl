{
  "name": "sensitive",
  "version": "0.0.0-dev",
  "config": {},
  "resources": {
    "sensitive:index:sensitive": {
      "properties": {
        "sensitiveOutput": {
          "type": "string",
          "secret": true
        },
        "sensitiveString": {
          "type": "string",
          "secret": true
        },
        "untypedValue": {
          "type": "object",
          "description": "No type constraint, so it defaults to the object (any) type."
        }
      },
      "type": "object",
      "inputProperties": {
        "sensitiveString": {
          "type": "string",
          "secret": true
        },
        "untypedValue": {
          "type": "object",
          "description": "No type constraint, so it defaults to the object (any) type."
        }
      },
      "requiredInputs": [
        "sensitiveString"
      ],
      "isComponent": true
    }
  }
}
